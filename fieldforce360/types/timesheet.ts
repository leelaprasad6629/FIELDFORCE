export interface Timesheet {
    id: string;
    userId: string;
    projectId: string;
    hoursWorked: number;
    date: string;
    description: string;
    createdAt: string;
    updatedAt: string;
}